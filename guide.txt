# Quiz App Deployment Guide - Vercel + MongoDB

## Prerequisites
- GitHub account
- Vercel account (free tier works)
- MongoDB Atlas account (free tier works)

---

## Part 1: Set Up MongoDB Database (5 minutes)

### Step 1: Create MongoDB Atlas Account
1. Go to https://www.mongodb.com/cloud/atlas/register
2. Sign up for free
3. Choose the **FREE** M0 tier
4. Select your preferred cloud provider and region
5. Click "Create Cluster"

### Step 2: Get Connection String
1. Click "Connect" on your cluster
2. Choose "Connect your application"
3. Copy the connection string (looks like: `mongodb+srv://username:<password>@cluster.mongodb.net/`)
4. Replace `<password>` with your actual password
5. Save this string - you'll need it later!

### Step 3: Allow Network Access
1. Go to "Network Access" in left sidebar
2. Click "Add IP Address"
3. Click "Allow Access from Anywhere" (or add `0.0.0.0/0`)
4. Click "Confirm"

---

## Part 2: Prepare Your Project Files

### Step 1: Create Project Folder
Create a folder called `quiz-app` on your computer with this structure:

```
quiz-app/
├── package.json
├── vercel.json
├── vite.config.js
├── .gitignore
├── public/
│   └── index.html
├── src/
│   ├── App.jsx
│   └── index.js
└── api/
    ├── get-categories.js
    ├── save-categories.js
    └── db.js
```

### Step 2: Copy Files
Copy each file from the code artifact I provided above. Make sure to:
- Copy the COMPLETE `src/App.jsx` from the working artifact (including all components)
- Create all API files exactly as shown
- Don't forget the config files!

---

## Part 3: Deploy to Vercel

### Step 1: Push to GitHub
1. Go to https://github.com/new
2. Create a new repository called `quiz-app`
3. In your terminal (inside the quiz-app folder):

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/quiz-app.git
git push -u origin main
```

### Step 2: Deploy on Vercel
1. Go to https://vercel.com/signup
2. Sign up with GitHub
3. Click "Add New Project"
4. Import your `quiz-app` repository
5. **IMPORTANT:** Add Environment Variable:
   - Click "Environment Variables"
   - Name: `MONGODB_URI`
   - Value: Your MongoDB connection string from Part 1
   - Click "Add"
6. Click "Deploy"
7. Wait 2-3 minutes for deployment

### Step 3: Your App is Live! 🎉
Vercel will give you a URL like: `https://quiz-app-xxxxx.vercel.app`

---

## Part 4: Testing Your Deployment

1. Visit your Vercel URL
2. Try logging in as a user
3. Click "Admin Login" (password: `admin123`)
4. Add some categories and questions
5. Test taking a quiz

---

## Troubleshooting

### Issue: "Cannot connect to database"
**Solution:** Check your MongoDB connection string in Vercel environment variables

### Issue: "Module not found"
**Solution:** Run `npm install` in your project folder before deploying

### Issue: API routes not working
**Solution:** Make sure your `vercel.json` file is in the root directory

### Issue: Page shows blank screen
**Solution:** Check browser console (F12) for errors. The Tailwind CDN should load.

---

## Customization Options

### Change Admin Password
In `src/App.jsx`, line 274:
```javascript
if (password === 'admin123') { // Change 'admin123' to your password
```

### Hide Admin Button
In `src/App.jsx`, line 7:
```javascript
const SHOW_ADMIN_BUTTON = false; // Change to false
```

### Change Default Questions
Edit the `getDefaultCategories()` function in `src/App.jsx`

---

## Custom Domain (Optional)

1. Go to your Vercel project settings
2. Click "Domains"
3. Add your custom domain
4. Update DNS records as instructed by Vercel

---

## Ongoing Maintenance

### Update Content
Just log in as admin and edit through the interface!

### View Database
Go to MongoDB Atlas → Browse Collections → See your data

### Redeploy
Push changes to GitHub, Vercel auto-deploys!

```bash
git add .
git commit -m "Updated questions"
git push
```

---

## Cost Breakdown

- **MongoDB Atlas Free Tier:** $0/month (512 MB storage)
- **Vercel Free Tier:** $0/month (100 GB bandwidth)
- **Total:** **FREE** ✅

---

## Need Help?

Common commands:
```bash
# Install dependencies
npm install

# Run locally
npm run dev

# Build for production
npm run build
```

Your quiz app is now live and fully functional with a real database! 🚀